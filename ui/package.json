{
  "name": "aurax-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build --config vite.client.config.ts && vite build --config vite.experimental.config.ts"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "preact": "^10.25.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.0.0"
  }
}
